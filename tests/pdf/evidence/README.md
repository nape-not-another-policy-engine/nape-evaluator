# PDF Example Note

The PDF authoring example uses a controlled text-extraction seam in automated tests instead of storing a heavyweight binary artifact in the repository.

The copyable Python test fixture still lives in:

- `tests/pdf/test_of_detail/verify_author_complete.py`

The focused proving test lives in:

- `tests/pdf/test_pattern_library.py`
